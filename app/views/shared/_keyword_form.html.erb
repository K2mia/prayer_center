
<%= form_for( @keyword ) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>

  <div class='field'>
    <%= f.text_field :keys, placeholder: 'Enter new search keys' %>
  </div>

  <%= f.submit 'Create search keys', class: 'btn btn-large btn-primary' %>
<% end %>
